# Optional.
# If this is missing, the app still retrieves source chunks,
# but answer generation falls back to a simple extractive response.
OPENAI_API_KEY=
# Optional model name for OpenAI Responses API.
OPENAI_MODEL=gpt-4.1-mini
