from __future__ import annotations


def simple_rerank(results: list[dict]) -> list[dict]:
    """Placeholder reranker.

    Chroma already returns nearest-neighbor results.
    Later we can add a CrossEncoder reranker here.
    """
    return results
